meta {
  name: Add manager
  type: http
  seq: 1
}

put {
  url: {{url}}/admin/manager
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

body:json {
  {
    "employeeId": 1,
    "restaurantId": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
