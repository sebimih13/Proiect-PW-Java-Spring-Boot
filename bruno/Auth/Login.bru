meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username}}",
    "password": "FixBambucea12"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
