meta {
  name: Remove an employee
  type: http
  seq: 7
}

put {
  url: {{url}}/employee/manager/remove?id=8
  body: none
  auth: bearer
}

params:query {
  id: 8
}

auth:bearer {
  token: {{tokenManager}}
}

settings {
  encodeUrl: true
  timeout: 0
}
