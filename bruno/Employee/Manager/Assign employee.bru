meta {
  name: Assign employee
  type: http
  seq: 6
}

put {
  url: {{url}}/employee/manager/assign
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenManager}}
}

body:json {
  {
    "employeeId": "10",
    "role": "COOK"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
